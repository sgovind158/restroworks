import LoadingCom from "@/views/components/StateHandle/Loading";

/**
 * Loading component that renders a loading indicator.
 * This component is typically used as a fallback UI while content is being loaded asynchronously.
 *
 * @returns {JSX.Element} The loading indicator component.
 */
export default function Loading() {
  return <LoadingCom />;
}
